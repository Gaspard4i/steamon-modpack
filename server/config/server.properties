motd=§6Steamon§r - Create x Cobblemon
view-distance=8
simulation-distance=6
max-players=20
spawn-protection=0
allow-flight=true
enable-command-block=true
difficulty=normal
gamemode=survival
pvp=true
spawn-animals=true
spawn-monsters=true
online-mode=true
white-list=false

# HDD optimization: avoid blocking fsync on every chunk write (big gen speedup on rotational disks; backups cover the small crash risk)
sync-chunk-writes=false
